---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
disable-model-invocation: true
license: MIT
---

Run a `grilling` session, using the `domain-modeling` skill throughout.

Before starting in Pi, load both dependency skills:

- `grilling`
- `domain-modeling`

When upstream wording says `/grilling` or `/domain-modeling`, treat those as skill names, not shell commands.
